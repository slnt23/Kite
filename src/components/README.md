




```src/
├── layouts/              ← 放 FrontLayout.vue 这类布局文件
├── components/
│   ├── site/             ← 放 SiteHeader、SiteFooter、MenuPanel 等网站专属组件
│   ├── common/           ← 放通用组件（以后复用率高的）
│   └── ui/               ← 以后放 Element Plus 二次封装的组件
├── views/
│   └── front/            ← 你现在的 LandingHomeView、LoginView 等页面
├── router/
└── utils/```